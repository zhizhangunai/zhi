"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const util_1 = require("../util");
const intl_messageformat_parser_1 = require("intl-messageformat-parser");
function verifySkeleton(ast) {
    for (const el of ast) {
        if (intl_messageformat_parser_1.isPluralElement(el) || intl_messageformat_parser_1.isSelectElement(el)) {
            const { options } = el;
            for (const selector of Object.keys(options)) {
                verifySkeleton(options[selector].value);
            }
        }
        if ((intl_messageformat_parser_1.isDateElement(el) || intl_messageformat_parser_1.isTimeElement(el)) &&
            intl_messageformat_parser_1.isDateTimeSkeleton(el.style)) {
            intl_messageformat_parser_1.parseDateTimeSkeleton(el.style.pattern);
        }
    }
}
function checkNode(context, node, importedMacroVars) {
    const msgs = util_1.extractMessages(node, importedMacroVars);
    for (const [{ message: { defaultMessage }, messageNode, },] of msgs) {
        if (!defaultMessage || !messageNode) {
            continue;
        }
        try {
            verifySkeleton(intl_messageformat_parser_1.parse(defaultMessage));
        }
        catch (e) {
            context.report({
                node: messageNode,
                message: e.message,
            });
        }
    }
}
const rule = {
    meta: {
        type: 'problem',
        docs: {
            description: 'Disallow offset in plural rules',
            category: 'Errors',
            recommended: false,
            url: 'https://github.com/formatjs/formatjs/tree/master/packages/eslint-plugin-formatjs#supported-datetime-skeleton',
        },
        fixable: 'code',
    },
    create(context) {
        let importedMacroVars = [];
        return {
            ImportDeclaration: node => {
                const moduleName = node.source.value;
                if (moduleName === '@formatjs/macro' || moduleName === 'react-intl') {
                    importedMacroVars = context.getDeclaredVariables(node);
                }
            },
            JSXOpeningElement: (node) => checkNode(context, node, importedMacroVars),
            CallExpression: node => checkNode(context, node, importedMacroVars),
        };
    },
};
exports.default = rule;
//# sourceMappingURL=supported-datetime-skeleton.js.map